<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.planmymeetings">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlanMyMeetings">
        <activity android:name=".screens.add_note.AddNoteActivity" />
        <activity android:name=".screens.add_appointment.AddAppointmentActivity" android:screenOrientation="portrait"/>
        <activity android:name=".screens.appointment_details.AppointmentDetailsActivity" android:screenOrientation="portrait"/>
        <activity android:name=".screens.appointments.AppointmentsActivity" />
        <activity android:name=".screens.login.LoginActivity" />
        <activity android:name=".screens.register.RegisterActivity" android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>